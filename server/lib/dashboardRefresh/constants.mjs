export const EPIC_PAST_DUE_MODES = new Set(["most_recent_done_date", "project_end_date", "either"]);

export const DUE_BY_ISSUES_CAP = 200;

export const VALID_DUE_BY_FIELDS = new Set([
  "initial_done_date",
  "most_recent_done_date",
  "due_date",
]);
